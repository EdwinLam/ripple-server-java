task helloWorld << {
    println "Hello World!"
}

task copy(type: Copy, group: "Custom", description: "Copies sources to the dest directory") {
    from "src"
    into "dest"
}

dependencies {
    compile project(':ripple-server-service')
}